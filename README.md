# Python-Stock-Price-Tracker
Build a Python desktop application called Stock Price Tracker using the Tkinter library for the GUI.  The program should allow a user to enter a stock ticker symbol (for example AAPL, TSLA, RELIANCE.NS, TCS.NS) and fetch the latest stock price using the Python library yfinance.
